# Workflow: New Component

1. Check if existing component can be reused.
2. Define one responsibility.
3. Define placement and allowed contexts.
4. Define states: default, hover, focus, disabled, loading, error.
5. Define keyboard behavior.
6. Define accessibility requirements.
7. Add to design system only if reusable.
