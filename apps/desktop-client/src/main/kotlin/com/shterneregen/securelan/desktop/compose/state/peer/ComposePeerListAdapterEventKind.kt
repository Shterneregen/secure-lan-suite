package com.shterneregen.securelan.desktop.compose.state.peer

public enum class ComposePeerListAdapterEventKind {
    PEER_DISCOVERED,
    PEER_LOST,
    PEER_SELECTED,
    PEER_DESELECTED,
    PEER_TARGET_CHANGED,
    PEER_LIST_REFRESHED,
}
